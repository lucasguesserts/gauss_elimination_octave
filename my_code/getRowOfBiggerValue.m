function rowIndex = getRowOfBiggerValue(expandedMatrix, currentColumn)
	numberOfRows = size(matrix,1);
	biggestValue    = expandedMatrix(currentColumn,currentColumn);
	biggestValueRow = currentColumn;
	for row = currentColumn+1 : numberOfRows 
	end
end
